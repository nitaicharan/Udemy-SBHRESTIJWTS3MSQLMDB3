spring:
  datasource:
    url: jdbc:mysql://localhost:3306/udemysbhrestijwts3msqlmdb3dev
    username: root
    password: root

  jpa:
    database: mysql
    hibernate.ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate.format_sql: true

  mail:
    host: smtp.gmail.com
    username: ${UDEMYSBHRESTIJWTS3MSQLMDB3_USERNAME:}
    password: ${UDEMYSBHRESTIJWTS3MSQLMDB3_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls.enable: true
          ssl.enable: true
          socketFactory:
            port: 465
            class: javax.net.ssl.SSLSocketFactory
            fallback: false