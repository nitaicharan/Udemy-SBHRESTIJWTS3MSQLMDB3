spring:
  datasource:
    url: jdbc:mysql://localhost:3306/test
    username: root
    password: root

  jpa:
    hibernate.ddl-auto: none
    show-sql: false
    properties:
      hibernate.format_sql: true